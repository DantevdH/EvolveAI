<!-- 637e1488-c400-4273-970b-956df98c7b53 db6c4607-5228-4cfb-a6ab-80f383d29a5b -->
# Plan: Relocate Plan Feedback to Main Training Screen

1. Audit & Simplify Onboarding Flow

- Update `frontend/src/components/onboarding/ConversationalOnboarding.tsx` to stop driving the preview chat step (remove `PlanPreviewStep` usage and preview transitions).
- Adjust `frontend/src/hooks/useAppRouting.ts` so users with a plan but `planAccepted` false land on `/(tabs)` instead of the onboarding preview.

2. Upgrade Floating Chat Experience

- Extend `frontend/components/FloatingChatButton.tsx` to auto-open the chat once when `authState.trainingPlan` exists and `planAccepted` is false, using the plan’s `aiMessage` as the opening message.
- Ensure closing the chat (or any acceptance actions inside it) invokes `updateUserProfile({ planAccepted: true })` and updates local state so the chat doesn’t auto-open again.

3. Port Plan-Feedback Logic into ChatModal

- Replace the placeholder handler inside `frontend/components/ChatModal.tsx` with the real plan-feedback workflow from `PlanPreviewStep` (call `trainingService.sendPlanFeedback`, update plan context, surface AI responses, honor `navigate_to_main_app`).
- Accept optional initial messages so FloatingChatButton can seed the plan intro.

4. Cleanup & Validation

- Remove any orphaned onboarding preview UI and ensure routing/tests still pass (no references to `PlanPreviewStep`).
- Verify first-time login opens chat automatically, API interactions behave as before, and subsequent visits respect `planAccepted`.